apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../secrets/                     # link to the folder with kustomization.yaml which link to secret. Security error - kustomize does not allow referencing files above the current directory!
  - 01-statefulset.yaml
  - 02-service.yaml
  - 03-service-headless.yaml     
  # - 04-job-init-replica.yaml     

# configMapGenerator:                 # creates a ConfigMap automatically from a file
#   - name: mongodb-init-cm
#     namespace: database
#     files:
#       - mongo-init.js=init-scripts/mongo-init.js