# Regular Service — single IP for frontend
# used by applications to connect to backend
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: backend
spec:
  type: ClusterIP
  ports:
  - port: 5000
  selector:
    app: backend-app