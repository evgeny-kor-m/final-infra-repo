apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-cm
  namespace: frontend
data:
  config.js: |
    window.APP_CONFIG = {
      API_URL: '/api',
      APM_ENABLED: true,
      APM_SERVER_URL: 'http://localhost:8200'
    }

