apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jenkins
  namespace: jenkins-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-srv
  template:
    metadata:
      labels:
        app: jenkins-srv
    spec:
      # securityContext:
      #       fsGroup: 1000 
      #       runAsUser: 1000
      serviceAccountName: jenkins-admin

      
      # initContainer installs plugins BEFORE jenkins starts
      initContainers:
        - name: install-plugins
          image: jenkins/jenkins:lts-jdk21
          command:
            - jenkins-plugin-cli
            - --plugins
            - "kubernetes generic-webhook-trigger workflow-aggregator git github configuration-as-code"
          env:
            - name: JAVA_OPTS
              value: "-Xmx512m"
          resources:
            requests:
              cpu: "200m"
              memory: "400Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
          volumeMounts:
            - name: jenkins-vol
              mountPath: /var/jenkins_home


      containers:
        - name: jenkins
          image: jenkins/jenkins:lts-jdk21
          imagePullPolicy: Always
          env:
          - name: JAVA_OPTS
            value: "-Xms256m -Xmx512m -XX:+UseG1GC"  # mem for JVM from 512mb-2gb
          resources:
            requests:
              memory: "400Mi"
              cpu: "200m"      
            limits:
              memory: "700Mi"  
              cpu: "500m"      
          ports:
            - name: http-port
              containerPort: 8080
            - name: jnl-pport
              containerPort: 50000
          livenessProbe:
            httpGet:
              path: "/login"
              port: 8080
            initialDelaySeconds: 120      # было 90 - дать больше времени на полный старт
            periodSeconds: 20             # было 10 - реже проверять
            timeoutSeconds: 15            # было 5 - больше времени на ответ при нагрузке
            failureThreshold: 8           # было 5 - больше попыток перед убийством
          readinessProbe:
            httpGet:
              path: "/login"
              port: 8080
            initialDelaySeconds: 90       # было 60
            periodSeconds: 20             # было 10
            timeoutSeconds: 15            # было 5
            failureThreshold: 5           # было 3

          volumeMounts:
            - name: jenkins-vol
              mountPath: /var/jenkins_home   

  volumeClaimTemplates:                     # auto-creates PVC for each pod
  - metadata:
      name: jenkins-vol
    spec:
      accessModes: [ "ReadWriteOnce" ]      # single node read-write
      resources:
        requests:
          storage: 1Gi