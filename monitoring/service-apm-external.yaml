apiVersion: v1
kind: Service
metadata:
  name: apm-server-external
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: apm-server-apm-server   # проверь актуальный label
  ports:
    - port: 8200
      targetPort: 8200
      nodePort: 30820